export type Profile = {
    id: string;
    displayName: string;
    photoURL: string;
    createdAt: string;
    description: string;

};
export type Photo = {
    id: string;
    name: string;
    url: string;
    
};